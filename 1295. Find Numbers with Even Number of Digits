class Solution {
    public int findNumbers(int[] nums) {
        int c=0;
        for(int i=0;i<nums.length;i++)
        {
            int val=nums[i];
          int digcount=(int)(Math.log10(val))+1;
            if(digcount%2==0)
                c++;
        
        }
        return c;
    }
}
